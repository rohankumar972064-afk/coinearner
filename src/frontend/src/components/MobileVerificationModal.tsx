// MobileVerificationModal has been removed — mobile verification is now the login flow itself.
// This stub prevents import errors in files that haven't been updated yet.
export function MobileVerificationModal(_props: {
  open: boolean;
  onDismiss: () => void;
  onVerified: () => void;
}) {
  return null;
}
